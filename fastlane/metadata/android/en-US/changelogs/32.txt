Multisig wallets: paste your Bitkey descriptor export exactly as exported (both the External and Internal lines) and Bag now watches change addresses too, so your balance no longer drops after you spend. Receive-only descriptors are flagged. Sentinel alerts now show the real amount sent (change excluded, fee included) instead of the whole input, and a brand-new wallet's first receive address is watched from the start.
